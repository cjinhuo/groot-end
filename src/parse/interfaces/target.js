export async function listSupplyDateUsingGET({ supplyId }) {
  return request(`/${isApi}/ops/supply/listSupplyDate/${supplyId}`, {
    method: 'GET'
  })
}

const name = export async function
  formatter(name, params, path)
const a = `export async funtion ${name}(${params}) {
  return request(`${isApi}${path}`,{
    method: ${method},
    data: params
  })
}`